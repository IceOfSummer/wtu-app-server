<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.xds.wtuapp.im.database.MessageReceiveMapper">

    <update id="increaseMessageCounter">
        UPDATE message_receive SET receive_id = receive_id + 1 WHERE uid = #{userId}
    </update>

    <update id="updateUserReceivedMsgId">
        UPDATE message_receive
        SET receive_id = #{receivedId}
        WHERE uid = #{userId}
          AND receive_id <![CDATA[ <= ]]> #{receivedId}
    </update>
</mapper>