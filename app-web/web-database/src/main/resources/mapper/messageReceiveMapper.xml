<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.xds.wtuapp.web.database.mapper.MessageReceiveMapper">

    <update id="increaseMessageCounter">
        UPDATE message_receive SET unreceived_id = unreceived_id + 1 WHERE user_id = #{userId}
    </update>

    <update id="updateUserReceivedMsgId">
        UPDATE message_receive
        SET receive_id = #{receivedId}
        WHERE user_id = #{userId}
          AND unreceived_id <![CDATA[ <= ]]> #{receivedId}
    </update>
</mapper>