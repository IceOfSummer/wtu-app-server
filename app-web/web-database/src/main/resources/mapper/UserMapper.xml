<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.xds.wtuapp.web.database.mapper.UserMapper">

    <cache eviction="FIFO" flushInterval="60000" size="512" readOnly="false"/>

    <sql id="user_base_query_col">
        u.user_id,u.name,u.credit,u.nickname
    </sql>

    <select id="findByUsername" resultType="pers.xds.wtuapp.web.database.bean.User">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="selectNameAndEmail" resultType="pers.xds.wtuapp.web.database.bean.User">
        SELECT nickname, email FROM user WHERE user_id = #{uid}
    </select>

    <select id="selectAllByIds">
        SELECT <include refid="user_base_query_col"/> FROM user u
        WHERE user_id IN
        <foreach collection="ids" item="item" open="(" separator="," close=")">#{item}</foreach>
    </select>

    <select id="selectByIdForQuery">
        SELECT <include refid="user_base_query_col"/> FROM user u
        WHERE user_id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true">
        INSERT INTO user(username,password,wtu_id,name,class_name) VALUE(#{u.username},#{u.password},#{u.wtuId},#{u.name},#{u.className})
    </insert>

</mapper>
